doctype html
html(lang="en")
    head
        include partials/header
    body.login 
        .contenedor-formulario 
            h1= nombrePagina 
            p Agrega tu correo para reestablecer tu Contraseña

            if locals.mensajes
                .alertas 
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`) 
                                p=error

                                
            form.caja-login(method="POST" action="/reestablecer")
                .campo 
                    label(for="email") Correo 
                    input(type="text" name="email" autocomplete="off" placeholder="Ingresa tu correo")
                .campo.enviar 
                    input.boton(type="submit" value="Reestablecer")
                .campo.acciones
                    a(href="/iniciar-sesion") Volver al Login 
